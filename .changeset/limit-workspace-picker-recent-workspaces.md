---
"@moonshot-ai/kimi-web": patch
---

Limit the empty-composer workspace picker to the five most recent workspaces and add a "More workspaces" expander so the "Add workspace" action stays visible on the first screen.
